# 🤖 DevManual-AI: Supervisor Agent

LangGraph의 '에이전트 슈퍼바이저' 패턴을 기반으로 구현된 자율형 AI 에이전트입니다. 사용자의 질문 의도를 분석하여, 로컬 문서 검색(RAG), 코드 생성, 웹 검색 등 다양한 도구를 스스로 선택하고 조합하여 최적의 답변을 생성합니다.

## ✨ 핵심 기능 (Core Features)

- **🧠 멀티 에이전트 슈퍼바이저 (Multi-Agent Supervisor):**
    - '감독관(Supervisor)' AI가 사용자의 복합적인 요청을 분석하고, 가장 적합한 전문가(RAG, CodeGen, Web Search)에게 작업을 동적으로 할당합니다.
    - 한 도구의 실행 결과가 불충분하다고 판단되면, 다른 도구를 연이어 호출하는 **자기 수정(Self-Correcting)** 로직을 통해 답변의 완성도를 높입니다.
- **📚 RAG (Retrieval-Augmented Generation):**
    - `docs` 폴더 내의 기술 문서(.md, .py 등)를 기반으로 `FAISS` 벡터 저장소를 구축합니다.
    - AI가 로컬 지식 베이스를 참조하여, 특정 기술의 개념이나 사용법에 대해 논리적이고 사실에 기반한 답변을 제공합니다.
- **💻 코드 생성 (Code Generation):**
    - 사용자의 요구사항에 맞춰 Python 코드, Markdown 문서 등 특정 형식의 결과물을 동적으로 생성합니다.
    - RAG로 검색된 관련 문서를 참고하여, 더 정확하고 맥락에 맞는 코드를 작성합니다.
- **🌐 ReAct 기반 웹 검색 (ReAct-based Web Search):**
    - `DuckDuckGo` 검색 도구를 활용하여, RAG 지식 베이스에 없는 최신 정보나 실시간 데이터를 즉각적으로 찾아내어 답변에 활용합니다.

## 🏛️ 아키텍처 (Architecture)

본 프로젝트는 LangGraph의 '상태 머신(State Machine)'을 기반으로 한 **에이전트 슈퍼바이저** 패턴을 따릅니다.

                  `+-------------------+
                  |   User Request    |
                  +-------------------+
                          |
                          v
+-----------------------------------------------------+
|                  StateGraph Workflow                |
|                                                     |
|           +-----------------------+                 |
|     +---->|  Supervisor Agent Node  |-----+         |
|     |     | (Decides next action) |     |         |
|     |     +-----------------------+     |         |
|     |               | (Tool Call)       | (END)   |
|     |               v                   |         |
|     |     +-----------------------+     |         |
|     +-----|    Tool Executor Node   |<----+         |
|           | (Runs RAG, CodeGen, etc.) |             |
|           +-----------------------+                 |
|                                                     |
+-----------------------------------------------------+
                          |
                          v
                  +-------------------+
                  |   Final Answer    |
                  +-------------------+`

1. 사용자의 요청은 `Supervisor Agent` 노드로 전달됩니다.
2. `Supervisor`는 요청을 분석하고, 사용할 도구를 결정하여 `Tool Executor` 노드를 호출합니다.
3. `Tool Executor`는 지시받은 도구(RAG, 웹 검색 등)를 실행하고, 그 결과를 `Supervisor`에게 다시 보고합니다.
4. `Supervisor`는 보고받은 결과를 검토하여, 답변이 충분하면 워크플로우를 **종료(END)**하고, 추가 정보가 필요하면 다른 도구를 호출하기 위해 다시 **`Tool Executor`로 돌아가는 루프(Loop)**를 형성합니다.

## 🛠️ 기술 스택 (Tech Stack)

- **AI & Backend:** Python, LangChain, LangGraph, Azure OpenAI
- **Frontend:** Streamlit
- **Vector DB:** FAISS (Facebook AI Similarity Search)
- **Tools:** DuckDuckGo Search

## 🚀 실행 방법 (How to Run)

1. **가상환경 생성 및 활성화:**Bash
    
    `python -m venv .venv
    source .venv/bin/activate  # macOS/Linux
    # .\.venv\Scripts\activate  # Windows`
    
2. **의존성 설치:**Bash
    
    `pip install -r requirements.txt`
    
3. **환경 변수 설정:**
    - 프로젝트 루트에 `.env` 파일을 생성합니다.
    - 아래 내용을 파일에 추가하고, 자신의 Azure OpenAI 키 값으로 교체합니다.
    
    `GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY"`
    
4. **RAG 데이터베이스 구축:**Bash
    - `docs` 폴더에 원하는 기술 문서(.md, .py 등)를 추가합니다.
    - 아래 명령어를 실행하여 FAISS 벡터 저장소를 생성합니다.
    
    `python -m scripts.build_rag`
    
5. **Streamlit 앱 실행:**Bash
    
    `streamlit run app.py`
    
    웹 브라우저에서 자동으로 앱이 실행됩니다.
    

## 📝 사용 예시

- **RAG 질문:** `LangGraph의 StateGraph에 대해 설명해줘.`
- **웹 검색 질문:** `OpenAI의 최신 소식 알려줘.`
- **복합 질문 (Multi-Agent):** `LangChain의 최신 버전은 뭐야? 그리고 그걸 사용해서 RAG 에이전트를 만드는 기본 파이썬 코드를 짜줘.`